/**
 * 랜딩 페이지 placeholder — ui-builder가 Phase 0에서 실제 랜딩으로 교체.
 * 섹션: 히어로 → 문제 공감 → 작동 3단계 → 가격 티저 → CTA 2개 → 대기자 모달 (PLAN.md Phase 0-3)
 */
export default function LandingPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-2xl font-bold">
        앱 설치 없이, 전화 한 통으로
      </h1>
      <p className="text-text-muted">랜딩 페이지 준비 중 (Phase 0)</p>
    </main>
  );
}
