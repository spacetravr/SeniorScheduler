/**
 * "예시 데이터" 라벨 — 아직 실데이터로 교체되지 않은(Phase 3+) 영역 표시용.
 * 색·라운드는 토큰만 사용.
 */
export function SampleBadge() {
  return (
    <span className="inline-flex items-center rounded-base border border-text-muted px-2 py-0.5 text-[11px] font-medium text-text-muted">
      예시 데이터
    </span>
  );
}
